package com.sailpoint.extended.controller;

/**
 * Interface for pointing package with all custom rest controllers.
 * Do not implement it, custom rest controller package must be equals or inherit current package
 */
public interface CustomRestController {
}
